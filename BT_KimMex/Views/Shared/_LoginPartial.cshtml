@using Microsoft.AspNet.Identity
@using BT_KimMex.Class;
@if (Request.IsAuthenticated)
    {
        string userId = User.Identity.GetUserId();
        using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
        {
            @Html.AntiForgeryToken()

<ul class="nav navbar-nav navbar-right">
    @*<li>
        <a href="@Url.Action("ResetPassword","Account",new { id = userId, status="ChangePassword" })"><i class="fa fa-user"></i> @User.Identity.GetUserName()</a>
    </li>*@
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><i class="fa fa-user"></i> @User.Identity.GetUserName()</a>
        <div class="dropdown-menu" style="padding:10px !important;">
            <a class="dropdown-item" href="@Url.Action("ResetPassword","Account",new { id = userId, status="ChangePassword" })"><i class="fa fa-key"></i> Reset Password</a><br/>
            <a class="dropdown-item" href="@Url.Action("ResetSignature","User",new { id = userId })"><i class="fa fa-vcard"></i> Reset Signature</a>
        </div>
    </li>
    @if (User.IsInRole(Role.SystemAdmin))
    {
        @*<li><a href="/Home/ComingSoon"><i class="fa fa-cog"></i></a></li>*@

        <li><a href="@Url.Action("Index","User")"><i class="fa fa-users"></i> USER</a></li>
    }

    <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
</ul>
    }
}
else
{
    @*<ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>*@
}
